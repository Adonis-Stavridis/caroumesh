import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { Caroumesh } from '../src/components/caroumesh';
import { Model } from '../src/components/model';
import { Lights } from '../src/components/lights';

import { Vector3 } from 'three';

<Meta title="Caroumesh" component={Caroumesh} />

export const Template = (args) => <Caroumesh {...args} />;

# Caroumesh

A simple way to showcase 3d models in a carousel-like way.

<Canvas>
  <Story name="Default">
    <Caroumesh shadows controls stats theme={'dodgerblue'}>
      <Model src="assets/Star destroyer.glb" scale={0.1} shadows />
      <Model src="assets/TIE fighter.glb" scale={5} shadows />
    </Caroumesh>
  </Story>
</Canvas>
