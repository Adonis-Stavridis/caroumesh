import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { Caroumesh, Model } from '../src/index';
import { Children } from './stories.constants';

<Meta title="Caroumesh" component={Caroumesh} />

export const Template = (args) => <Caroumesh {...args} />;

# Caroumesh

A simple way to showcase 3d models in a carousel-like way.

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      children: Children,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Styled

<Canvas>
  <Story
    name="Styled"
    args={{
      shadows: true,
      controls: true,
      theme: 'dodgerblue',
      children: Children,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Single

<Canvas>
  <Story
    name="Single"
    args={{
      shadows: true,
      controls: true,
      theme: 'dodgerblue',
      children: Children[0],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
