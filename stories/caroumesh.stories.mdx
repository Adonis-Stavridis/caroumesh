import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { Caroumesh } from '../src/components/caroumesh';
import { Model } from '../src/components/model';

<Meta title="Caroumesh" component={Caroumesh} />

export const Template = (args) => <Caroumesh {...args} />;

# Caroumesh

A simple way to showcase 3d models in carousel-like way.

<Canvas>
  <Story name="Default">
    <Caroumesh>
      <Model src="assets/Star destroyer-transformed.glb" />
      <Model src="assets/TIE fighter-transformed.glb" position={[30, 0, 0]} />
    </Caroumesh>
  </Story>
</Canvas>
